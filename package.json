{
  "name": "pg-hurtowniedanych-2025_26",
  "type": "module",
  "private": true,
  "scripts": {
    "db:up": "cd database && docker-compose up -d",
    "db:down": "cd database && docker-compose down",
    "db:del": "cd database && docker-compose down -v",
    "db:logs": "cd database && docker-compose logs -f",
    "dev": "cd generator && uv run main.py"
  }
}